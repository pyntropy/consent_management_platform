# Получить сборку согласий по атрибутам

GET /client/api/v1/consent-bundle?<key>=<value>

1 — составить из параметров запроса список атрибутов
2 — выбрать из таблицы consents_bundle_attrs id сборки
3 — выбрать из таблицы